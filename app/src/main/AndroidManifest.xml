<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.progtechuc.ingatpajak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IngatPajak">
        <activity android:name=".HasilPPnBMActivity"></activity>
        <activity android:name=".PPN_BMActivity" />
        <activity android:name=".hasilPBBActivity" />
        <activity android:name=".PbbActivity" />
        <activity android:name=".output_pph" />
        <activity android:name=".input_pph" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>